{{#partial "contenido"}}

	<table class="table table-stripped">
		<tr>
			<th>Nombre</th>
			<th></th>
		</tr>
		
		{{#each}}
			<tr>
				<td>{{name}}</td>
				<td><a href="{{url}}">Ver</a></td>
			</tr>
		{{/each}}
	</table>

{{/partial}}

{{>layout.hbs}}